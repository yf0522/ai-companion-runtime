# Demo smoke evidence — dry-run template

Date: 2026-07-09  
Mode: dry-run (no live server)  
Command: `python scripts/demo_smoke.py --dry-run --record`

This file is a **template evidence artifact**. Replace with a live `--record` run
against a running API before treating it as diligence proof.

## Expected live checklist

1. Auth register/login elder JWT
2. `/ws/chat` medication reminder utterance
3. `/ws/chat` scam utterance → risk path
4. `GET /api/notifications`
5. `GET /api/traces/{trace_id}`

## Dry-run result

- passed: `True` (structure only)
- Does **not** prove reminder persistence, scam classification, or notification delivery
